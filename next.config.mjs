/** @type {import('next').NextConfig} */
const nextConfig = {
    icons: {
        icon: '/favicon.ico',
      },
    images:{
        domains:["i.ibb.co", "ss.ancovabd.com"]
       } 
};

export default nextConfig;
